import { styles } from '../../../config'

const {
    colors,
    grid,
} = styles

export default {
    wrapper: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        height: 160,
    }
}