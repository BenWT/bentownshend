import React from 'react'
import cx from 'classnames'
import is from 'react-jss'

import style from './style'

// TODO: Build Team component
const Team = ({
    content
}) => (
    <div className={cx('container')}>
        <h3>Team</h3>
    </div>
)

export default is(style)(Team)