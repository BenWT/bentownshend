import React from 'react'
import cx from 'classnames'
import is from 'react-jss'

import style from './style'

// TODO: Build Locations component
const Locations = ({
    content
}) => (
    <div className={cx('container')}>
        <h3>Locations</h3>
    </div>
)

export default is(style)(Locations)