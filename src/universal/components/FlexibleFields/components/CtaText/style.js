import { styles } from '../../../../../config'

const {
    colors
} =  styles

export default {
    column: {
        width: 720,
        maxWidth: '100%',
        textAlign: 'center',
    },
    backgroundText: {},
}