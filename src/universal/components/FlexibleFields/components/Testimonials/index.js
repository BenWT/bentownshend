import React from 'react'
import cx from 'classnames'
import is from 'react-jss'

import style from './style'

// TODO: Build Testimonials component
const Testimonials = ({
    content
}) => (
    <div className={cx('container')}>
        <h3>Testimonials</h3>
    </div>
)

export default is(style)(Testimonials)